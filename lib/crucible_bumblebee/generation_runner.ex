defmodule CrucibleBumblebee.GenerationRunner do
  @moduledoc """
  Placeholder boundary for Bumblebee generation integration.

  Slice 1 uses the forward runner and logits processor adapter. Full generation
  wrapping belongs here after the trace path is stable.
  """
end
